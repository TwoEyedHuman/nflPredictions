import sys


#establish db connection

def main(args):
  #load in schedule data from database as list: games = []
  
  #load in probability data (if not random)
  
  #loop through until max runs is reached (froms args)
  #on each loop, simulate each game and place results back into object
  #write results to database
  
  #print descriptive statistics and exit
  #runtime
  #number of runs


if __name__ == "__main__":
  main(sys.argv)
